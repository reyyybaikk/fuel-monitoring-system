services:
  web:
    type: web
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      # Add your environment variables in Render UI; keep them secret.
      NODE_ENV: production
